name: Deploy to GitHub Pages

on:
  push:
    branches: [claude/interval-timer-app-r54e6e, claude/logistics-network-analysis-tool-tmei0z]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Cache-bust local script/style tags
        run: |
          sed -i -E \
            -e "s#(src=\"js/[^\"]+\.js)\"#\1?v=${GITHUB_SHA}\"#g" \
            -e "s#(href=\"css/style\.css)\"#\1?v=${GITHUB_SHA}\"#g" \
            index.html
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - id: deployment
        uses: actions/deploy-pages@v4
